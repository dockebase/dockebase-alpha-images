# Dockebase Configuration
# Copy this file to .env and fill in the values

# Domain for Dockebase UI (e.g., panel.example.com)
# Use IP address for self-signed HTTPS, or localhost for HTTP
DOMAIN=localhost

# ACME email for Let's Encrypt SSL certificates
# Required only when PROXY_MODE=https-acme
ACME_EMAIL=

# Base URL for authentication (include protocol)
# Examples: https://panel.example.com, https://192.168.1.100, http://localhost
BASE_URL=http://localhost

# Authentication secret (generate with: openssl rand -hex 32)
AUTH_SECRET=

# Encryption key for sensitive data (generate with: openssl rand -hex 32)
DOCKEBASE_ENCRYPTION_KEY=

# Instance secret (auto-generated on first startup if empty)
# DOCKEBASE_INSTANCE_SECRET=

# Reverse proxy provider: caddy or traefik
PROXY_PROVIDER=caddy

# Proxy mode: http, https-selfsigned, https-acme
# http             - HTTP only (localhost or behind Cloudflare Tunnel)
# https-selfsigned - Self-signed certificate (IP-based access)
# https-acme       - Let's Encrypt automatic HTTPS (requires domain)
PROXY_MODE=http
